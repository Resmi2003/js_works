var num = 30;      // num=30
                            //   True      True
if(num%3==0 && num%5==0){   // 30%3==0 && 30%5==0     True
    console.log("The number is divisible by both 3 and 5.");    // prints The number is divisible by both 3 and 5.
    
}

else{
    console.log("The number is not divisible by both 3 and 5.");
    
}